<%= simple_form_for([:backend, @category]) do |f| %>

  <div class="form-group">
    <%= f.input :name, class: 'form-control', placeholder: "目錄名稱", id: "InputName" %>
  </div>

  <div class="form-group">
    <%# f.simple_fields_for :cover do |attach| %>
     <%# attach.input :attachment, as: :file, class: 'file' %>
    <%# end%>
  </div>

  <%= f.button :submit, "送出", class: 'btn btn-primary', data: { disable_with: "..." } %>
<% end %>
